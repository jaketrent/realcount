<div class="pg poll-pg">
  <a href="#poll/{{ title_slug }}/vote" class="btn btn-large btn-primary pull-right">Vote &raquo;</a>
  <h1>{{ title }}</h1>
  <p>{{ desc }}</p>

  <div id="chart"></div>

  <h2>Choices</h2>
  <div class="row-fluid" id="opts">
    {{#each opts}}
    <div class="span6">
      <a href="javascript:void(0);" class="btn btn-large disabled">{{ votes }} - {{ title }}</a>
    </div>
    {{/each}}
  </div>
</div>