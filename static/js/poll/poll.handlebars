<div class="pg poll-pg">
  <a href="#poll/{{ title_slug }}/vote" class="btn btn-large btn-primary pull-right">Vote &raquo;</a>
  <h1>{{ title }}</h1>
  <p>{{ desc }}</p>

  <h2>Chart</h2>
  <div id="chart"></div>

  <h2>Choices</h2>
  <ul id="opts">
    {{#each opts}}
      <li>
        <a href="#" class="btn btn-large disabled span6">{{ votes }} - {{ title }}</a>
      </li>
    {{/each}}
  </ul>
</div>